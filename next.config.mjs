/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
      reactRoot: true, // Usually enabled by default in App Router
    }
  };
  
  export default nextConfig;
  